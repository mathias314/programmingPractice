#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

void function1(vector<int> a);

int main()
{
	unsigned int t, theSize;
	int nextElement;
	vector<int> a;

	cin >> t >> theSize;
	/* 
	for(int i = 0; i < theSize; i++)
	{
		cin >> nextElement;
		a[i] = nextElement;
	}

	switch(t)
	{
		case 1:
			// call function 1
			function1(a);
			break;
		case 2:
			// call function 2
		case 3:
			// call function 3
		case 4:
			// call function 4
		case 5:
			// call function 5
	}
	*/
	return 0;
}


void function1(vector<int> a)
{
	// p, choose 2
	// check if the sum is 7777
	// if it is, print yes and exit
	// if it breaks without finding the sum, print no and exit
}

