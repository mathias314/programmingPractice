#include "../../reverse.h"

/*
void linkedList::recUtil()
{
    if( headptr != nullptr )
    {
        return;
    }
    recReverse(headptr, nullptr, *headptr)
}

void linkedList::recReverse( node* curr; node* prev; node** head)
{
    if( curr == nullptr )
    {
        headptr = prev;
    }

    next = 
}
*/
